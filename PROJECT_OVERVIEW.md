# KidLoop 项目文档

> 文档状态：产品需求基线与已确认实现记录
>
> 产品名称：KidLoop
>
> 更新日期：2026-09-09

## 线路设计基线

线路、自动学生名单、学校接送批次共享、每日接送核对及日历试算统一按 [线路与每日接送核对](docs/route-planning.md)。网页、服务端和生产数据已按此流程更新，验收结果见文末。学生已有学校和课外班关联；线路不再提供逐学生上下车选择。

## 当前实现状态

- 管理者手机端性能（2026-09-11）：修复“今日”页未读取 SQL 布尔表达式结果、导致已有当日任务时仍重复生成排班的问题，并合并学校、排班和学生页的重复数据读取。相同本地 production build、同一数据库，以 iPhone User-Agent 连续测量完整响应：“今日”由 4.027–4.866 秒降至首次 1.921 秒、后续 0.513–0.531 秒；“学校”0.266–0.394 秒，“排班”0.290–0.656 秒，“学生”0.179–0.248 秒，“资料”0.090–0.147 秒。当前 Vercel 线上版本同样连续测量三轮：“今日”首次 2.310 秒、后续 0.860–0.862 秒；其余四个入口 0.291–0.735 秒。五个入口均达到 2–3 秒目标；物理 iPhone 仍需实机复测。

- Pickup／Drop-off 操作历史增加设备位置快照，批量送达同时记录每名学生；定位失败仅记原因，不校验位置或阻止接送。详见 [操作位置记录](docs/operation-locations.md)。iPhone 用途说明需更新原生包，尚未实机验收。

### 两校学生照片核查与 Ellis 恢复（2026-09-08）

- McAuliffe 10 名在册学生的私有照片关联完整，线上图片接口均返回有效图片；未发现可复现的缺失，未改其照片数据。
- Ellis 5 名学生仍使用旧内嵌卡通 PNG，线上学生页面约 28.47 MB。按原始备份的学生 ID 一一对应恢复原 JPEG，存入私有 Blob 并更新照片接口链接；只改照片和更新时间，姓名、学校、家长关联及接送安排均保持原值。备份、上传记录与操作脚本保存在 Git 忽略的 `.local-data/ellis-photo-repair/`。
- 修复后 Ellis 页面约 100 KB；生产地址 390px 浏览器确认 McAuliffe 10/10、Ellis 5/5 图片加载成功，关联司机的 10 张与 5 张照片接口均返回 200。Test 账号沿用既有卡通替代策略。本次为数据库照片恢复，无需应用代码发布；未进行物理 iPhone 实机验证。

学生直接关联学校和目标课外班，班级为学生个人的可选文字字段（如 B102），不再独立创建或选择班级对象。管理员可为学生勾选每周不接送的星期；固定线路生成日名单时排除这些学生。学校学期日历按日期或日期范围记录上课、放假、家长会周和提前放学，日程中的年级时间直接覆盖常规规则；学生每周不接送与学校日历分开。

运营学期作为接送方的工作空间：管理员统一设置起止日期，各学校自动初始化日期和可编辑的美国联邦假日，学校页只能编辑本校日期。学期结束后整体归档学校规则、学生名单、线路与执行记录，日常界面清空，基础资料保留。新学期可沿用每周年级规则、线路草稿和待核对学生，不复制旧日期、假期或执行状态；司机车辆重新绑定。当前版本一个工作学期，归档后创建下一期。详细需求、复制矩阵和验收标准见 [运营学期需求](docs/operating-terms.md)，优先于旧的逐校校历存档说明。

第一版基础功能已实现：多车辆管理、司机管理与排班、学校与课外班接送资料、完整学生清单、学生行程安排、Pickup/Dropoff 状态流转及状态历史。

当前实现使用 Node.js、Next.js、TypeScript 和 PostgreSQL，提供响应式桌面与移动 Web 界面，并已通过 Capacitor 生成仅支持 iPhone 的 `KidLoop.xcodeproj` 原生壳。已支持管理员、司机、家长三种角色的邮箱密码登录、服务端访问隔离和受控学生照片上传。老师端、推送通知和弱网队列仍属于后续实现范围。

下文编号章节保留产品范围与验收目标，不代表所有目标已经实现；具体交付以“已实现”章节及文末交付记录为准。

系统界面支持简体中文和英文，默认显示中文。用户可在全局导航中切换语言，选择结果通过 `kidloop_locale` Cookie 保存；日期、状态、表单、操作反馈和页面元数据必须与当前语言保持一致。

界面采用 Web 桌面端与 iPhone 端两套布局，共享数据、服务端操作及中英文支持。桌面端保留侧栏、多列资料和展开的管理表单；iPhone／窄屏端使用顶部品牌与账号菜单、底部五项导航（今日、学校、线路、学生、资料）、单列行程与学生卡片，并将新增表单折叠为按需展开的入口。桌面基础样式位于 `src/app/globals.css`，手机布局独立维护在 `src/app/mobile.css`（920px 及以下启用触屏导航，680px 及以下启用手机卡片布局）。手机输入字号至少 16px，主要触控操作至少 44px，页面预留顶部和底部安全区。


## 登录与家长工作区（已实现）

- 管理员登录后进入运营首页，可在电脑端 Web 后台「账号管理」中创建三类账号、绑定司机或多个孩子、调整孩子绑定、停用账号及重置密码。
- 司机登录后进入 `/driver`，只查询分配给本人的行程和学生清单，可处理接送状态并查看相关家长留言。
- 家长登录后进入 `/parent`，只查询明确绑定的孩子，显示个人资料、所选日期日程、当天接送状态、司机和车辆、实际上车和送达时间。默认日期为洛杉矶当地今天，可选择其他日期。
- 家长可设置今天或未来某一天缺席，或保存不超过 1000 字的特殊要求。没有已发布行程时也能提前设置；管理员在所选日期看到请假和留言，已有行程的司机也能看到。
- 家长请假会将尚未上车的关联任务改为缺席，保留操作者和状态历史。取消请假只恢复由家长请假产生的缺席；司机手动记录的缺席不会被覆盖。已上车（包括上车后异常）的任务不能改为家长缺席。
- 管理员和司机可将“已接到”改为“缺席”，用于上车后家长临时接走等情况；保留原上车时间和状态变更历史，并计入当次行程已处理人数。
- 创建行程时排除当天已请假的孩子；家长留言不自动修改路线、地址或接送人，需要双方确认。页面可见且未操作表单时每 30 秒刷新状态，不代表已发送推送通知。
- 密码使用带随机盐的 scrypt 散列；数据库会话只保存随机令牌的摘要，Cookie 使用 HttpOnly、SameSite=Lax 和正式环境 Secure，登录页默认勾选“记住登录 30 天”，所建会话及 Cookie 保留 30 天；取消勾选则使用浏览器会话 Cookie，服务端最多保留 12 小时。仅成功登录后保存邮箱提示 180 天，主动退出后仍可带出邮箱，取消勾选并成功登录会清除该提示。密码通过系统密码管理器自动填充，不写入应用的 Cookie 或本地存储。退出、停用和重置密码会撤销相应会话。登录按账号限制 15 分钟内最多 10 次尝试。
- 登录账号与历史 `parents` 联系人分开管理，只有 `user_students` 明确绑定授予孩子访问权限，姓名、邮箱或电话号码匹配不会自动授权。

## 1. 项目定位

KidLoop 是一套连接家庭、学校、课外班和接送方的儿童出行协同管理产品。系统中的运营数据归属于学校、课外班或接送方三种组织，并通过组织间的合作关系完成接送协同。

产品不只是记录一次“接送”，而是管理孩子在“学校 -> After School -> 兴趣班 -> 家”等地点之间反复发生的日常出行，包括多辆车、司机排班、孩子安排、行程执行、接送状态和家长通知。

英文定位：

**After-School Pickup & Family Ride Management**

品牌口号：

**Safe Rides. Simple Schedules. Happy Families.**

## 2. 产品目标

- 让接送方配置线路站点、时间、司机和车辆，由系统自动带出学生，按学校日历自动安排每日接送，只在需要时调整。
- 让司机清楚看到自己的任务、路线、乘车名单和每个停靠点。
- 让家长及时了解孩子的接送安排和当前状态。
- 让老师掌握孩子的到校、离校和交接状态，并参与必要的交接确认。
- 通过明确的确认和异常流程，减少漏接、错接、迟到及信息不同步。
- 保留完整、可追溯的行程和操作记录，便于查询与责任确认。

## 3. 用户角色

### 3.1 管理者

- 管理所属组织、组织成员及本组织有权维护的数据。
- 管理孩子、家庭联系人、老师、司机、车辆和地点。
- 创建固定接送计划和临时接送任务。
- 安排司机、车辆、孩子及停靠顺序。
- 查看当日运行总览并处理冲突、迟到、缺席等异常。
- 查询历史行程和操作记录。

### 3.2 司机

- 查看本人当日及未来的接送任务。
- 查看路线、停靠点、时间要求、孩子名单及必要备注。
- 更新到达、上车、下车、完成和异常状态。
- 在执行关键动作时确认对应孩子，避免错接或漏接。

### 3.3 家长

- 查看孩子的接送日程、司机和车辆信息。
- 接收接送状态及异常通知。
- 提交请假、临时改地址、取消或变更接送的申请。
- 确认孩子已安全送达；具体确认机制后续确定。

### 3.4 老师

- 查看本人负责的班级、课程和孩子名单。
- 查看相关孩子的计划到达、实际到达和离开状态。
- 在学校或课外班接送点确认孩子的交接。
- 上报孩子缺席、未到达、无人接收或其他交接异常。
- 仅查看工作职责所需的孩子与行程信息。

## 4. 组织与数据关系

### 4.1 组织类型

- **学校（School）**：第一版只管理学校名称、班级和学生；接送所需资料放在独立的学校 Pickup 配置中。
- **课外班（After-School Program）**：第一版只管理课外班名称及司机 Dropoff 所需资料，不建设复杂的课程教务数据。
- **接送方（Transportation Company）**：关联管理者、司机、车辆、服务区域、路线和行程。

### 4.2 数据归属与协作

- 每一条组织业务数据必须有明确的所属组织，不允许成为无归属的共享数据。
- 管理者、司机和老师通过组织成员关系获得权限；家长通过家庭与孩子关系获得权限。
- 同一个用户可以加入一个或多个组织，并在不同组织中拥有不同角色。
- 同一个孩子可以同时关联家庭、学校、一个或多个课外班。
- 学校或课外班可以与一个或多个接送方建立接送服务关系。
- 一次行程可以连接多个组织，但必须明确服务提供方、接送来源、接送目的地及相关孩子。
- 跨组织数据默认不可见，只有建立有效合作关系且业务确有需要时才开放最小范围的数据。
- 组织合作关系的建立、停用和权限变化必须保留审计记录。

### 4.3 第一版数据范围

学校、班级、老师和课外班资料以完成接送业务为准，不扩展为学校管理或课外班教务系统。

| 数据对象 | 第一版字段与要求 |
| --- | --- |
| 学校 | 学校主体只保留名称；另关联学校 Pickup 配置。 |
| 学校 Pickup 配置 | 学校地址、Pickup 地图、Pickup 要求、按星期设置的计划放学时间。 |
| 班级 | 班级名称及所属学校；不管理课程表、成绩等教务数据。 |
| 老师 | 姓名、所属学校或课外班、关联班级；登录所需联系方式按账号需要保存。 |
| 学生 | 照片、姓名、班级、年级、年龄、所属学校、目标课外班及家长信息。学生是第一版的完整核心资料。 |
| 家长 | 姓名、与学生关系、主要联系电话、备用联系电话、邮箱及必要的接送备注。 |
| 课外班 | 名称、地址、Dropoff 信息、课外班要求。 |

学校 Pickup 地图是供司机识别停车入口、候车区、行车方向和集合点的图片或文件，不等同于普通地图导航地址。Dropoff 信息用于说明课外班的下车位置、入口、接收人或交接方式。

## 5. 核心业务对象

- **组织（Organization）**：名称、类型及状态；学校与课外班的接送资料通过独立配置关联。
- **组织成员（Organization Membership）**：用户、组织、角色、权限范围及成员状态。
- **组织合作关系（Organization Partnership）**：学校或课外班与接送方的服务关系、有效期及状态。
- **学校 Pickup 配置（School Pickup Profile）**：学校地址、Pickup 地图、Pickup 要求和计划放学时间。
- **班级（Classroom）**：班级名称、所属学校及学生名单。
- **孩子（Child）**：照片、姓名、班级、年级、年龄、家庭、学校、目标课外班及必要接送备注。
- **家庭（Family）**：家长姓名、与孩子关系、联系电话、备用联系电话、邮箱、通知偏好及关联孩子。
- **老师（Teacher）**：姓名、所属学校或课外班及关联班级；第一版不保存非接送业务所需的扩展档案。
- **课外班 Dropoff 配置（Program Dropoff Profile）**：地址、Dropoff 信息和课外班要求。
- **司机（Driver）**：身份信息、联系方式、资质状态、可工作时间及当前状态。
- **车辆（Vehicle）**：车牌、车型、座位数、可用状态、保险或年检信息。
- **地点（Location）**：家庭、学校、课后班、兴趣班及临时上下车点。
- **固定计划（Recurring Schedule）**：按星期重复的常规接送需求。
- **行程（Trip）**：某一天、某辆车和某位司机执行的一组接送任务。
- **停靠点（Stop）**：行程中的地点、计划时间、实际时间和上下车孩子。
- **接送任务（Ride Assignment）**：孩子在一次行程中的起点、终点和执行状态。
- **Pickup 学生清单（Pickup Manifest）**：按学校和行程生成的学生 Pickup 名单及逐人确认状态。
- **异常（Incident）**：迟到、缺席、地址变更、车辆故障、无法联系等事件。
- **交接记录（Handoff）**：孩子在司机、家长或老师之间的交接双方、时间、地点和结果。
- **通知（Notification）**：发送给管理者、司机、家长或老师的状态消息和提醒。

## 6. 核心业务流程

当前确认的完整需求及实现边界见 [已确认需求](docs/confirmed-requirements.md)。涉及老师、多组织合作、通知和签名的早期规划项尚未实现；以下固定线路流程取代旧的每日人工调度方案。

### 6.1 建立组织与基础资料

1. 管理者建立学校、课外班或接送方组织，并添加组织成员。
2. 管理者录入学校名称、班级、简化老师资料，以及完整的学生和家长资料。
3. 管理者配置学校地址、Pickup 地图、Pickup 要求和每周计划放学时间。
4. 管理者录入课外班名称、地址、Dropoff 信息和课外班要求。
5. 接送方管理者录入司机、车辆、服务区域及常用地点。
6. 相关组织建立接送服务合作关系。
7. 管理者为孩子设置常规每周接送需求和必要注意事项。

### 6.2 固定安排与自动每日任务

1. 管理者设置各学校学期、假期及年级接送时间，同一规则可勾选多个年级。
2. 设置开始时间、学校及课外班站点顺序，绑定司机车辆；学校和放学时段自动带出学生。
3. 启用前完成学校规则及司机车辆配置；安排后统一核对遗漏、冲突和分段容量。
4. 周/月日历先做试算，详情按需读取；正式任务沿用同一规则生成，跳过假期和不适用学生，无需手动创建每日车次。
5. 司机和家长打开对应页面查看任务和状态；“今天”页用于执行与异常查看。

### 6.3 司机执行行程

1. 司机查看按学校分组的 Pickup 学生清单。
2. 司机查看学校名称、地址、Pickup 地图、Pickup 要求和计划放学时间。
3. 司机到达学校后更新状态，并按清单逐个确认学生 Pickup 结果。
4. 前往课外班前，司机查看课外班名称、地址、Dropoff 信息和课外班要求。
5. 到达课外班后，司机按清单逐个确认学生 Dropoff 和交接结果。
6. 系统向相关家长、老师和管理者同步关键状态。
7. 所有学生均有明确结果后结束行程。

### 6.4 孩子交接

1. 司机到达学校、课外班或家庭接送点。
2. 司机逐个确认准备上车或下车的孩子。
3. 相关家长或老师根据业务要求完成交接确认。
4. 系统记录交接双方、时间、地点和结果，并更新孩子接送状态。

### 6.5 临时变更与异常处理

1. 家长按日期设置缺席或特殊要求留言，不能用请假覆盖已上车事实。
2. 管理者可修改固定线路司机，影响未开始和后续任务；当前没有只对某一天替班的独立操作。
3. 已上车后家长临时接走，由司机或管理员标记缺席；保留上车时间和变更历史。
4. 已执行任务保留原司机、站点和状态，资源冲突在今日页提示。推送通知、独立交接确认仍为后续范围。

## 7. 状态设计

### 7.1 行程状态

- `DRAFT`：草稿，仍可调整。
- `PUBLISHED`：已发布，司机和家长可见。
- `IN_PROGRESS`：司机已开始执行。
- `COMPLETED`：所有必要接送任务已完成。
- `CANCELED`：行程已取消。
- `NEEDS_ATTENTION`：存在需要调度处理的异常。

### 7.2 孩子接送状态

- `SCHEDULED`：已安排。
- `DRIVER_EN_ROUTE`：司机正在前往接送点。
- `ARRIVED_FOR_PICKUP`：司机已到达接送点。
- `PICKED_UP`：孩子已上车。
- `ARRIVED_AT_DROPOFF`：车辆已到达送达点。
- `DROPPED_OFF`：孩子已下车并完成交接。
- `ABSENT`：孩子缺席。
- `CANCELED`：本次接送已取消。
- `EXCEPTION`：发生异常，需要人工处理。

状态更新必须记录操作者、时间和必要的位置或备注，历史记录不得被普通用户直接覆盖。

## 8. MVP 功能范围

### 8.1 管理者端

- 当日及按周日历视图。
- 管理所属组织、成员、合作组织及其服务关系。
- 创建、复制、编辑和发布行程。
- 为行程分配司机、车辆和孩子。
- 调整停靠点顺序和计划时间。
- 显示司机时间冲突、车辆冲突和超载风险。
- 展示当日各行程进度及异常。

### 8.2 司机端

- 登录并查看本人任务。
- 查看按学校和停靠点分组的 Pickup 学生清单。
- 查看学校名称、地址、Pickup 地图、Pickup 要求和计划放学时间。
- 查看每位学生的照片、姓名、班级、年级、年龄及必要家长联系信息。
- 查看课外班名称、地址、Dropoff 信息和课外班要求。
- 通过学校或课外班地址打开地图导航。
- 开始、暂停和完成行程。
- 按清单逐个更新到达、已 Pickup、已 Dropoff、缺席及异常状态。
- 在网络不稳定时保留待同步操作，恢复连接后自动重试。

### 8.3 家长端

- 查看孩子当前和未来接送计划。
- 查看已发布的司机、车辆及预计时间。
- 接收发布、即将到达、已接到、已送达和异常通知。
- 提交请假、取消及临时变更申请。

### 8.4 老师端

- 查看本人负责的班级、课程和相关孩子。
- 查看相关接送计划、预计到达时间和实际状态。
- 确认孩子与司机之间的接收或送出交接。
- 提交缺席、未到达、无人接收及其他异常。

### 8.5 基础资料管理

- 三种组织及组织成员管理。
- 学校、课外班与接送方的合作关系管理。
- 学校名称及学校 Pickup 配置管理。
- 简化的班级和老师管理。
- 完整的学生和家长资料管理。
- 课外班名称及 Dropoff 配置管理。
- 司机及可用时间管理。
- 车辆及座位数管理。
- 常用地点管理。
- 固定每周接送计划管理。

### 8.6 记录与查询

- 按日期、孩子、司机或车辆查询历史行程。
- 查看每次状态变更的时间线。
- 查看临时变更、异常及处理结果。

## 9. 关键业务规则

- 同一司机或车辆在时间重叠的行程中不能被重复分配。
- 任一时段内车辆承载的孩子数不能超过可用座位数。
- 每个孩子在同一时间段只能处于一个有效接送任务中。
- 行程发布后，影响司机、车辆、时间、地点或孩子名单的修改必须通知相关用户。
- 孩子上车和下车必须分别确认，不得仅以行程完成代替。
- 管理者只能管理所属组织及有效合作关系范围内的数据。
- 司机只能操作分配给自己的行程，具有相应权限的管理者可处理异常和必要的人工更正。
- 老师只能查看和操作本人负责班级、课程、地点及交接任务范围内的数据。
- 人工更正必须记录修改前后内容、操作者、时间和原因。
- 家长只能查看与本人家庭关联的孩子和行程。
- 跨组织访问必须同时满足有效合作关系、用户权限和当前业务任务三个条件。
- 孩子的敏感信息只向完成任务所必需的角色展示。
- 行程不能在仍有未处理孩子任务时直接标记为正常完成。

## 10. 产品与界面要求

- 调度端以高信息密度和快速操作为主，优先支持桌面和大屏使用。
- 司机端以移动设备单手操作为主，关键按钮需清晰且易于点击。
- 家长端重点突出“今天谁接、何时到、现在到哪一步”。
- 老师端重点突出“哪些孩子将到达、哪些孩子将离开、交接是否完成”。
- 所有角色看到的状态名称和含义必须一致。
- 关键页面必须包含加载、空数据、失败、离线和权限不足状态。
- 危险操作需二次确认；普通高频状态操作应减少不必要步骤。
- 时间、地址、孩子姓名和异常提示不得因屏幕尺寸而被截断到无法识别。
- 已确认支持简体中文和英文，默认简体中文；页面、表单、状态和反馈均须保持语言一致。

## 11. 安全与隐私要求

- 儿童位置、家庭地址、联系人及行程属于敏感数据。
- 使用基于角色和数据归属的访问控制，默认拒绝无关数据访问。
- 所有组织数据查询必须执行组织范围校验，不能只依赖客户端筛选。
- 所有客户端和服务端通信必须使用加密连接。
- 账号登录、关键资料修改和行程状态修改需要审计记录。
- 日志和分析数据不得包含不必要的儿童姓名、详细地址或联系方式。
- 图片、证件等文件应使用受控访问地址，不能作为公开静态资源暴露。
- 数据保留、删除、监护人授权和当地儿童隐私法规需要在上线地区确定后专项评审。

## 12. 非功能要求

- **可靠性**：关键状态操作应具备幂等性，重复提交不能产生重复接送记录。
- **实时性**：行程状态更新后，应尽快同步给调度端及相关家长。
- **弱网支持**：司机端在短时断网后不得丢失已确认的操作。
- **可追溯性**：发布、调整、状态更新和人工更正均需保留审计信息。
- **可扩展性**：数据模型应支持三种组织、多组织成员关系、跨组织合作、多个服务区域和不同类型接送路线。
- **可用性**：司机执行主流程时，应尽量避免输入长文本。
- **时间处理**：数据库统一保存明确时区的时间；界面按运营区域时区显示。
- **无障碍**：颜色不能是状态识别的唯一方式，交互元素应提供明确文字或图标标签。

### 12.1 系统架构要求

- KidLoop 采用 Web 优先架构，所有角色共用同一套 Web 业务系统和后端 API。
- 管理者主要通过桌面或平板浏览器使用系统。
- 司机、家长和老师使用响应式移动 Web 界面，第一版通过 Capacitor 原生薄壳封装为 iPhone App。
- App 壳层不重复实现业务页面和业务规则，只负责承载 Web 应用及桥接必要的设备能力。
- 学生照片拍摄或上传、消息推送、地图导航、网络状态、本地安全存储等能力通过 App 壳层桥接。
- 浏览器版和 App 版必须共享账号、权限、数据、状态定义及核心业务流程。
- 弱网待同步操作属于共享业务能力，不能只在某一个客户端单独实现。
- 详细边界记录在 `docs/architecture.md`。

### 12.2 已确定技术栈

- **运行时**：Node.js。
- **Web 与服务端框架**：Next.js。
- **开发语言**：TypeScript，前端、服务端及共享领域模型统一使用。
- **主数据库**：PostgreSQL，用于保存账号、组织、学生、家长、司机、车辆、计划、行程和审计等关系数据。
- 学生照片和学校 Pickup 地图等文件保存在对象存储中，PostgreSQL 只保存文件元数据和受控引用。

### 12.3 App 内链接跳转规范

- **已确认要求**：App 内的所有链接跳转必须始终留在 KidLoop App 内，不得唤起 Safari、Chrome 或其他外部浏览器。
- 站内页面使用 App 内导航；外部网页、地图链接及图片或文件预览使用 App 内浏览或预览界面。
- App 内浏览或预览界面必须提供返回或关闭入口，返回时保留原业务页面及登录状态。
- 普通链接、新窗口链接、脚本打开窗口和重定向均须遵守同一规范；无法在 App 内打开时，在 App 内提示，不自动转交外部浏览器。
- 此项为项目开发和验收规范，不表示现有实现已全部完成适配。
- 学校资料和行程卡片的地址地图、接送示意图已改为页内弹窗：提供关闭入口，外部内容使用隔离 iframe，禁止打开新窗口或导航顶层页面；不自动转交外部浏览器。
- 地址地图只表示学校或培训学校地址，不代表实际接送入口。未提供接送示意图时，紧凑行程卡隐藏占位提示，其他资料视图可显示待补充，不得生成空链接或用普通地址地图冒充接送图；新增学校时示意图 URL 可留空，有值时仅接受无账号密码的 HTTPS URL。
- 地图弹窗已覆盖桌面及手机宽度的浏览器测试；iPhone 原生 App 内实际跳转和返回仍需单独验收。

## 13. MVP 暂不包含

以下内容不作为第一版默认范围，确认业务需要后再排期：

- 自动路线优化和智能派单。
- 实时 GPS 连续追踪及地图轨迹回放。
- 费用计算、订阅、账单和在线支付。
- 司机工资、工时及财务结算。
- 车辆维修、油耗和完整车队资产管理。
- 学校 SIS 或第三方课后班系统集成。
- 短信、电话、WhatsApp 等多渠道通知。
- 生物识别、二维码或 NFC 上下车确认。
- 学校课程、成绩、考勤等完整教务管理。
- 课外班课程排课、报名、收费等完整教务管理。
- 老师履历、资质等与接送无关的扩展档案。

## 14. 建议的阶段划分

### 阶段 1：可用原型

- 完成三种组织、四类角色、基础资料、固定多站线路、自动每日任务和司机状态流转。
- 用测试数据验证一整天从排班到安全送达的流程。

### 阶段 2：小范围试运行

- 加入家长端、老师端、交接确认、通知、临时变更、弱网同步和审计记录。
- 使用真实但受控的少量路线验证权限、异常流程和现场操作效率。

### 阶段 3：正式运营

- 完成隐私与安全评审、监控告警、数据备份和运营报表。
- 根据试运行数据决定是否加入路线优化、实时定位及费用模块。

## 15. 验收基线

MVP 至少需要通过以下端到端场景：

1. 管理者建立学校、课外班、接送方及其合作关系，并添加对应组织成员。
2. 管理者能够用简化字段创建学校、班级和老师，并录入完整的学生及家长资料。
3. 管理者能够为学校配置地址、Pickup 地图、Pickup 要求和按星期设置的计划放学时间。
4. 管理者能够为课外班配置名称、地址、Dropoff 信息和课外班要求。
5. 系统生成当日需求，管理者完成分车、分司机和发布。
6. 冲突或超载安排能够被系统明确阻止或提示处理。
7. 司机能从 Pickup 清单查看学生照片及学校与学生的全部必要信息，并逐个核对、确认结果。
8. 司机能查看课外班 Dropoff 信息与要求，并逐个完成学生 Dropoff。
9. 老师能够查看相关孩子并完成学校或课外班接送点的交接确认。
10. 家长能够看到与本人孩子相关的安排和状态变化。
11. 家长临时取消后，管理者、司机、老师和行程名单保持一致。
12. 发生迟到或缺席时，系统能够记录、通知并由管理者处理。
13. 弱网期间的司机操作在恢复网络后能够正确同步且不重复。
14. 未授权用户无法查看其他家庭、无关班级或无合作关系组织的数据。
15. 历史记录能够还原一次行程的发布、变更、交接和实际执行过程。
16. 在 iPhone App 内点击站内页面、外部网页、学校或课外班地图、图片和文件预览链接，跳转及返回全程保留在 App 内，不唤起外部浏览器；新窗口链接和重定向同样通过验证。
17. “学校”与“线路”是独立模块；规则与学校日历在标签下方选择学校后立即过滤，不需额外提交。切换、刷新、返回均保持学校与数据一致，线路模块不出现学校筛选。
18. 学生卡片编辑、删除图标位于右上角，不单独占据底部一行；窄屏长姓名与按钮不重叠，删除仍须确认，历史记录不因归档而丢失。

## 16. 待确认事项

- 首发时以学校、课外班还是接送方作为主要签约和运营主体。
- 接送完成时是否需要监护人签名、PIN、照片或二维码确认。
- 是否需要实时 GPS，以及家长可见的定位粒度。
- 通知渠道优先级：App Push、短信、邮件或其他渠道。
- 正式运营地区及对应的儿童隐私与运输合规要求；界面语言已确定为简体中文和英文，当前测试地点位于美国加州。
- 临时变更的截止时间、审批规则和通知责任。
- 是否涉及收费、套餐、按次计费或司机结算。
- ORM、对象存储、部署平台和地图服务的具体选型尚未确定；Android App 不在第一版范围内。

## 17. 文档维护要求

- [自然语言联动重排需求](docs/natural-language-rescheduling.md)：记录复杂接送调整、后台排班试算、AI 工具调用和 token 成本统计要求，属于待实施需求。
- [自然语言接送排班联动调整 1.0](docs/natural-language-rescheduling-v1.md)：开发需求草案，不包含班级需求，全部业务规则沿用现有系统；明确自然语言输入、联动试算、确认保存及验收范围，尚未实现。

- 本文件作为 KidLoop 当前产品范围和关键业务规则的主文档。
- 已确认的需求变更必须同步更新本文件，避免代码与产品规则脱节。
- 未确认想法先放入“待确认事项”或后续规划，不直接写成已定规则。
- 系统架构记录在 `docs/architecture.md`；数据模型和 API 确定后，分别建立 `docs/data-model.md` 和 `docs/api.md`。
- 每次进入正式开发阶段前，应为对应范围补充可验证的验收标准。

## 统一资料入口与个人账号（2026-09-07）

- `/resources` 是车队、学校、培训学校的统一入口，页内按分类切换；原 `/fleet` 和 `/locations` 链接自动跳转。车辆和司机的新增、编辑、归档、排班以及学校和培训学校资料均保留。
- 系统账号管理位于 `/admin/accounts`，仅管理员有权限，入口只显示于桌面 Web 侧栏；iPhone 不显示系统账号管理入口，直接打开该地址会显示使用电脑后台的提示。原 `/accounts` 自动跳转。设备布局不是权限凭据，所有后台写操作仍校验管理员角色。
- 右上角账号图标打开账号菜单，显示当前姓名、邮箱和角色，提供个人资料、修改密码、中英文切换和退出。Web 侧栏底部也提供同一菜单。
- `/profile` 允许所有已登录用户修改自己的姓名、联系电话和登录邮箱。修改登录邮箱需验证当前密码；司机修改姓名或电话时同步接送名单中的司机资料。角色和关联关系不可在个人资料中修改。
- 修改密码必须验证当前密码及两次新密码（6–128 字符），成功后撤销该账号全部会话并重新登录。密码验证设尝试次数限制。
- 家长通过账号菜单或孩子卡片进入 `/parent/children`，只可编辑已绑定且在册孩子的姓名、年级、年龄、照片上传、家长联系方式和常用备注。服务器保留学校／班级／培训学校关联，不接受家长伪造的关联变更或删除指令；保留原照片、导入来源和并发版本检查。
- 新增迁移 `005_personal_profiles.sql`：账号联系电话及资料更新时间。

### 学生名册按学校查看

- `/students?school=<学校ID>` 按学校筛选学生与人数，默认第一所学校，无效参数回退第一所学校。刷新保留当前筛选。
- Web 和 iPhone 均以学校选择与学生列表为主体，添加班级、添加学生改为紧凑按钮和弹窗。
- 添加班级默认当前学校；添加学生仅显示当前学校的班级。原有学生编辑与删除保持不变。
- 学生卡片的编辑、删除图标位于姓名右侧的右上角；移除底部独立操作行，保留照片、班级年级、年龄、家长联系信息和送达地点，长姓名可换行，不与操作按钮重叠。
- 管理员可编辑或删除学生、车辆和司机。删除采用归档，保留历史行程与操作记录；有未完成关联任务时按已有保护规则阻止相关变更。详见 `docs/record-management.md`。

### 规则、线路与学校日历

- 主导航与页面标题“学校”（原“规则”）对应 `/schedule`，页内标签为“学校”和“日历”（英文 School / Calendar），分别维护学校规则和查看学校日历，原查询参数不变；“线路”独立为 `/routes`，列出全部固定线路，可以跨学校，不受学校筛选影响。移动端管理员导航为今日、学校、线路、学生、资料五项。
- 学校下拉框选中后立即在 App 内切换并过滤对应规则和日历，无需点击“查看”；保留当前标签及滚动位置，URL 保存学校选择，刷新和浏览器前进后退保持一致。切换加载期间暂时禁用选择框，避免重复提交。
- 学校选择框位于“学校”和“日历”标签栏下方；独立“线路”模块不显示学校选择框。原 `/schedule?tab=routes` 和 `/schedule/dispatch` 在 App 内重定向到 `/routes`，今日页入口同步使用新路径。
- 学期和假期绑定学校；年级可多选，共用一组星期和接送时间。同校同年级同星期不允许重复规则。
- 2026-09-07 用户确认的初始时间：Ellis、Stratford School 周二 12:45，周一、三、四、五 13:45；McAuliffe 周二 14:00，周一、三、四、五 14:35。使用可编辑的年级规则覆盖 TK、K、1–7 年级，周末不设接送。这是用户指定的测试默认值，并非官方学校放学时间。迁移 `017_pickup_grade_scope.sql` 清理当前运营学期旧规则中残留的 8–12 年级；保存及新学期沿用均校验年级范围，不改写存档历史。
- 当前三个学校默认美国联邦法定节假日不接送，已按 [OPM 2026–2027 日历](https://www.opm.gov/policy-data-oversight/pay-leave/federal-holidays/) 录入假期及补休日，包含 2027-12-31 的 2028 元旦补休；假期记录可编辑或删除。不推定学校春假、寒暑假或教师培训日；本轮用户另行确认学期为 8/20–12/20，当前已将 Ellis、McAuliffe、Stratford School 设置为 2026-08-20 至 2026-12-20（含首尾日期），不自动延伸到其他年份。
- `node --env-file=.env.local scripts/seed-school-calendar.mjs` 可预览上述默认数据，`--apply` 才提交；仅针对上述三个学校，事务内锁定学校、拒绝重叠并避免重复插入，不覆盖后续人工修改，不自动处理其他学校或 2028 年以后的假期，不更改历史行程。
- 线路标题按站点顺序自动生成简短名称（例如 `McAuliffe → One Stop → Stratford`）；超过三个站点时用首尾站点概括，重名自动编号。
- `fixed_routes` 保存有效日期、每周星期、固定司机和车辆、启用状态。线路可先保存为未启用，启用前配置学校学期、年级规则及司机车辆，学生自动带出，遗漏和冲突在每日接送核对中显示。
- 日历概览和未来计划使用只读试算，不生成正式车次。当天执行及正式任务读取按线路、校历和自动名单幂等生成或同步 `trips`、`trip_students`，司机看到完整多站线路，家长只看到自己孩子的接送。无需手动创建每日车次。
- 学校日历标记放假或提前放学；多学校线路仍可接送其他正常上课学校。日历日程调整学校站点时间，并保留后续站点至少原计划的行车间隔。车辆或司机不可用、当日资源冲突等情况在今日页提示管理员调整。
- 修改线路上的司机影响未执行及后续日期任务；已经发生司机操作的任务保留实际司机、站点和状态。修改固定司机属于持续变更，尚未提供仅当天替班按钮。
- 已有历史行程保留；学生已在其他未取消行程中时不重复安排，并显示提示。`/schedule/dispatch` 旧入口重定向到固定线路，原每日创建班次和行程的表单及写入入口已移除。
- 学校日历只表示当前学校每天是否上学、是否需要接送及接送时间，不是线路排班日历。颜色和详情仅依据该校学期、假期和年级接送规则，不查询或依赖固定线路、司机、车辆或学生分配；线路未启用也不影响学校日历。
- 绿色表示上学且需接送，黄色表示适用上学日的接送时间调整，红色表示已配置的放假日；周末休息及学期外为灰色，并显示文字说明。未设置学期或上学日缺少规则时标为“待设置”，不擅自判定放假；明确配置周末规则时可显示周末接送。学校日历不显示“当天没有已启用线路”等调度提示，不推断未配置的法定假日。
- 点击任意日期，在 App 内弹窗查看该学校当天详情：日期、学校、上学及接送状态、所属学期、假期或特殊时间说明、各适用年级接送时间。关闭后留在原月份和选中日期，不再要求滚动到月历下方查看详情。
- 测试：`KIDLOOP_TEST_DATABASE_URL=postgresql://<user>@localhost/postgres node --conditions=react-server --import tsx --test tests/fixed-routes.test.ts tests/pickup-settings.test.ts tests/pickup-calendar.test.ts tests/roles.test.ts`，使用本机隔离 schema。

### 历史资料与测试数据确认

以下为 2026-09-07 导入批次基线，不是实时在册人数；后续以 App 中的编辑、归档结果为准。

| 学校 | 用户确认的目的地 | 导入学生 | 已有照片 |
| --- | --- | --- | --- |
| Ellis | 小树苗（Little Tree Sunnyvale） | 5 | 5 |
| McAuliffe | One Stop Learning Center | 6 | 6 |
| Stratford School | 晨星中文学校 Saratoga 校区 | 8 | 0 |

- 合计 19 名学生、11 张原始照片。只选取原始资料中可确认的信息；未导入的名单、未知年龄、缺失家长联系信息与照片不编造、不用生成头像替代。
- 第三批名单经用户确认属于 Stratford School，目的地为晨星中文学校 Saratoga 校区；使用最终确认的学校与目的地名称，不再沿用早期输入的 `Straford` 或“晨新”。
- 学校与培训学校的公开地址、电话及网站来源保存在 `docs/test-location-sources.json`；该文件为当时查证记录，不代表实时校历，也不覆盖用户后来指定的测试接送时间。
- 儿童姓名清单、原图、导入清单和照片预览不放进公共项目文档或 Git，保存在受控数据库及被忽略的 `.local-data/`；导入流程见 `docs/test-roster-import.md`。
- 原测试场景为两车三线：TEST-01（14 座）承担 McAuliffe → One Stop 和 Stratford → 晨星 Saratoga；TEST-02（8 座）承担 Ellis → 小树苗，另建两名明确标记的测试司机。初始行程日期为 2026-09-07，时间仅供模拟。
- `scripts/seed-test-routes.mjs` 创建的是指定日期的历史测试行程，不是 `fixed_routes`。升级后的“线路”模块只显示固定线路，旧测试行程不会自动成为固定线路，也不会因此出现在后续每天的学校接送日历中。
- 学期起止日期、学校自己的寒暑假及特殊停课日、实际接送入口示意图、现场交接要求仍待补充；不能为了填满日历而推定实际开学日期或启用未经确认的线路。

### 本轮验证与交付记录

- `f23ab2b` 已提交并推送到 `origin/main`：独立“规则”和“线路”模块、学校自动筛选与选择框位置、旧线路入口兼容跳转、默认校历数据脚本、学生卡片右上角操作按钮。
- 默认校历数据已写入当时配置的数据库：三个学校共 6 条每周时间规则、69 条假期记录；重复运行预览不增加重复记录。脚本提交不等于会在新环境自动执行，其他环境需单独预览并确认应用。
- 已验证类型检查、提交内容快照的固定线路与日历测试；浏览器检查覆盖学校自动过滤、标签保留、刷新和返回、旧入口跳转、新线路表单，以及学生卡片在 320/390/1280 像素宽度下的布局和编辑/删除弹窗。
- 本轮提交未包含工作区另行修改的 App 图标、线路自动命名及输出素材。推送成功不等于已确认线上部署完成，也不等于新的 iPhone 原生安装包已发布；原生 App 地图跳转验收仍待完成。

### 学生照片上传

- 管理员添加／编辑学生、家长编辑已绑定孩子均支持文件选择和照片预览；iPhone 使用系统图片选择入口。选择后上传，保存资料后关联到学生。
- 浏览器将可解码照片（原图最多 15 MB）压缩为 JPEG 再上传；服务端限制为 1 MB，重新解码、纠正方向、缩至最长边 1024 并移除元数据。无法解码的 HEIC 提示转为 JPEG。
- 上传内容保存到 Vercel 私有 Blob；`student_photos` 只保存对象地址、上传人和学生关联。环境变量为 `BLOB_READ_WRITE_TOKEN`，迁移为 `007_student_photos.sql`。
- `/api/photos` 仅接受管理员／家长的同源上传。`/api/photos/[id]` 按角色和学生关系鉴权，返回私有不缓存图片；图片直接请求鉴权接口，不经 Next 图片优化公共缓存。
- 用户仅能将自己上传的照片关联到当前有权编辑的学生。取消选择不改变原照片；尚未关联的对象保留为上传草稿，当前没有自动清理任务。原有照片链接仍可显示。
- iPhone 原生配置已加入相机和相册用途说明；拍照权限配置需随下一次 App 构建发布。

- 上传交互参考 RedHotWeb 的 `packages/ui/src/components/image-upload.tsx` 和 `support-chat-widget.tsx`：照片预览、独立相册／拍照按钮、原生 Camera 插件、逐步压缩到 1 MB 以下，以及问题反馈页的拖拽／粘贴入口。KidLoop 按自身会话和孩子绑定关系鉴权，不复用 RedHotWeb 的公开图片地址或业务接口。
- Web 使用文件选择并支持拖拽／粘贴截图；iPhone App 安装 Camera 插件后使用系统相册／相机，取消选择保留原照片且不显示失败。原生插件不可用时保留文件选择兼容入口。原生配置变更需重新构建安装包。

### 今日页数据范围（2026-09-07）

- 今日页仅显示当天非草稿、非取消行程；学生和车辆统计来自这些行程，按学生、车辆去重，不使用全校在册学生或车队总量。无有效行程时统计归零、列表显示空状态。
- 已核对并取消 2026-09-07 的 3 条旧测试行程（测试司机 01/02、TEST-01/02，未绑定固定线路）；保留学生状态与操作历史，没有删除基础资料。

### 课外班英文显示名称

- 晨星中文学校（Morningstar Educational Group - San Jose Campus）统一显示为 `Morningstar San Jose`。
- 小树苗（Little Tree Montessori International School of Sunnyvale）统一显示为 `Little Tree Sunnyvale`。
- 已同步基础资料、固定线路站点和自动线路标题；地点 ID、地址及学生关联保持原值。

- 测试车辆显示为 `Test Vehicle 01/02`，测试司机显示为 `Test Driver 01/02`；原编号、车牌和线路绑定不变。
- 测试登录账号显示姓名统一为 `Test Admin`、`Test Driver`、`Test Parent`，不修改邮箱、密码或角色绑定。

## 学校全称与简称（2026-09-08）

- 学校基础资料保留全称 `schools.name`，新增可选简称 `schools.short_name`；普通页面、学校筛选、学生及家长资料、账号绑定和新线路站点优先显示简称，未填写时回退全称。学校资料页同时显示全称并提供名称编辑；管理员写入校验长度与并发版本。
- 已按用户名单更新 3 所既有学校并新增 5 所：Christa McAuliffe Elementary School（McAuliffe）、Cherry Chase Elementary School（Cherry Chase）、Cumberland Elementary School（Cumberland）、Ellis Elementary School（Ellis）、Stratford School（Stratford）、John Muir Elementary School（John Muir）、Murdock-Portal Elementary School（Murdock-Portal）、Eaton Elementary School（Eaton）。
- 保留原学校 ID、19 名学生与学校关联及既有接送配置。新学校地址、示意图、接送要求及每周时间待补充；只按现有机制初始化当前运营学期日期及可编辑的默认联邦假日，不推定官方放学时间。
- 改名同步当前学期线路模板站点与自动标题；已保存的执行快照和归档不改写，未开始任务由既有生成流程后续同步。
- 迁移 `022_school_short_names.sql`；名单脚本 `node --env-file=.env.local --conditions=react-server --import tsx scripts/update-school-roster.ts` 默认事务回滚预览，`--apply` 才写入。已应用配置数据库并再次预览确认无重复新增。

- 本次验证：类型检查、Lint、生产构建及学校名称、固定线路、运营学期 5 项测试通过；数据库只读复核 8 所学校、每校 1 个当前学期，原有学生关联不变。浏览器可加载登录页，但现有测试账号凭据未通过登录，尚未完成登录后的桌面/手机页面验收；未修改账号密码，未提交、推送或部署。

### 当前学期日期修正（2026-09-08）

用户确认 Fall 2026 运营学期为 **2026-08-20 至 2026-12-18（含首尾）**，取代原 12 月 20 日结束的基线。已在数据库同步 6 所仍为 12 月 20 日结束的学校，以及 2 条固定线路的结束日期；McAuliffe、Stratford 原已为 12 月 18 日结束。保留各校既有开学日：Stratford 为 8 月 24 日，其余 7 所为 8 月 20 日；线路起始日仍为 9 月 7 日。操作前核对 12 月 18 日之后无本期行程，事务提交后只读复核 8 所学校及两条线路均于 12 月 18 日结束。未改动学生、假期、每周接送时间或历史任务。

### 两校学生资料照片校正（2026-09-08）

按用户提供的 2026 年 9 月 McAuliffe → One Stop、Stratford → 晨星两张名单修正学生资料：McAuliffe 更新 5 名、补录 5 名，Stratford 更新 5 名、补录 3 名，共覆盖照片中的 18 名学生。修正姓名、年级、班级及照片明确标记的每周不接送星期；K/GK 统一存为 K，G3/G4/G5 存为 3/4/5。照片空白班级留空，缺失年龄、照片和家长信息不编造。

### McAuliffe 名单与今日任务同步（2026-09-08）

## 司机日程页（2026-09-08）

- 司机导航增加“日程 / Schedule”，路径 `/driver/week`；“我的行程 / My trips”保留日常状态操作。当前页面只高亮一个导航项，手机底部为两项入口。
- 默认按洛杉矶运营日期显示本周，横排“一、二、三、四、五”及对应日期；移除上周、下周与本周翻页按钮。点击星期只显示当天计划，工作日默认选中今天，周末默认周一；桌面和手机均使用横排星期选择。显示日期、行程数量、时间、车辆、学校到课外班的分段及学生/缺席人数。没有任务明确显示空状态，不把未配置日程推断为学校放假。
- 点击日期或查看当天任务进入 `/driver?date=...&week=...`，当天页提供返回原周及所选日期的入口，导航全程使用 App 内站内链接。日程页不提供接到、缺席或完成按钮，具体操作在当天行程中执行。
- 初版验证（横排星期调整前）：类型检查、Lint、生产构建和 8 项相关测试通过；隔离本机数据库的浏览器验证覆盖周一至周五、停课空状态、每周不接送人数、司机数据隔离、周切换及当天/返回原周导航。390px 手机宽度和 1440px 桌面布局无横向溢出。未进行 iPhone 原生 App 实机跳转验收，未提交、推送或部署。
- 横排星期调整后：类型检查、Lint 和生产构建通过；保留原有司机数据范围和计划读取规则，新增星期按钮及键盘左右切换，返回日程时恢复所选日期。本次未重新进行浏览器或 iPhone 实机验收。

### 日程月视图（2026-09-08）

- 同一个司机日程页提供周/月图标切换，无额外底部导航入口或可见切换文字；图标保留辅助读屏标签。周视图沿用横排工作日，月视图为周一开始的七列日历、数字年月和左右月份箭头。
- 月历按真实司机任务显示小点，点击日期在下方显示当天计划，进入当天任务再返回时保留月份、视图和日期。周末也可查看。复用原有司机鉴权，仅按选中日期读取生成详情，其余日期通过整月只读概览展示，不增加定时任务；历史空日期不推断为放假。
- 月视图验证：类型检查、Lint、生产构建与 3 项日期测试通过。独立本机数据库浏览器验证覆盖 30/31 天月份、停课/有任务标记、选中日期人数、返回原月及日期、月份箭头和周/月图标切换；390px 月历无横向溢出并完成截图检查。未进行 iPhone 原生 App 实机验收，未提交推送或部署。

### 日程切换等待反馈修正（2026-09-08）

- 线上日程近期请求返回 200，部署状态 Ready；发现原页面在周/月参数跳转时等待完整任务读取，缺少切换中的即时反馈。增加按视图/日期重置的 Suspense 边界，先显示目标日历及加载提示，再补齐任务；链接待跳转时图标显示进度。没有改变任务生成规则或生产数据。
- 类型检查、Lint、构建通过。在独立本机数据库持有线路事务锁期间，浏览器验证月历仍显示 30 天及加载状态；释放锁后计划标记正常出现，切回 5 个工作日通过，390px 截图检查通过。该测试证明等待中的切换反馈，不代表已确认用户设备的全部故障原因；尚未做原生 App 实机测试或部署此修复。

### 按登录账号显示学生照片（2026-09-08）

- 以登录邮箱的精确域 `test.kidloop.local` 识别现有 Test 账号，不依据显示姓名、角色或设备。Test 账号在学生列表、司机行程和家长页面统一显示 `/demo-avatars/student-teal.png`；服务端数据不下发原照片地址，照片预览接口在既有权限校验后也仅返回替代 PNG，不读取真实 Blob。
- 非 Test 账号保留原照片地址及照片权限，无照片时保留原空状态；不修改数据库照片或上传关联。网页及手机 App 使用同一规则。照片响应保持 private/no-store 并增加 Vary: Cookie，避免账号间复用。
- 类型检查、Lint、构建和 4 项照片相关测试通过；隔离本机数据库的 HTTP 集成验证覆盖桌面/iPhone User-Agent、Test 页面无真实 URL、非 Test 页面保留真实 URL、直接照片接口仅返回替代 PNG 和匿名 401。构建追踪确认替代图片打包到照片函数。此验证不等同于 iPhone 原生实机验收；未提交推送或部署。


## 自然语言接送排班联动调整（2026-09-08）

- 管理员入口 `/routes/adjust`，从线路页进入。支持文字及语音转文字、条件追问、联动试算、前后对照、明确确认、事务应用及调用记录；不包含班级需求，业务规则沿用现有系统。
- 共享每日排班的资格和时间推导，搜索日历日程、换人车、分组拆分及有现成时间依据的合并。结果通过日历日程和临时线路保存，不修改长期固定名单；版本复核、防重复提交、回滚及原接送历史保护已实现。
- 已执行和用户锁定任务保持保护；未开始的既有临时线路支持再次调整，仅替换指定日期，并保留其他日期安排、学生任务 ID、请假和历史。没有新增实际车程计算或消息推送。完整范围、配置及限制见 [实现与验证记录](docs/rescheduling-implementation.md)。
- 新增迁移 `023_rescheduling.sql` 和 `024_reschedule_replacements.sql`；仅在隔离本机测试库执行。真实 API 密钥及文本模型尚未配置，尚未验证实际模型效果。当前全局英文设置保持不变，新界面保留中英文文案。
- 类型检查、Lint、生产构建及隔离数据库 42 项测试通过，桌面及 390px 浏览器完成确认应用流程。本轮未提交、推送或部署。

- 无 API 对话模拟入口已补齐：`npm run reschedule:simulate -- --example` 可运行虚构示例；传入结构化指令可读取真实数据的只读快照或离线快照，输出前后方案和冲突，不写正式排班。详见 [实现与验证记录](docs/rescheduling-implementation.md)。

### 语言切换按 Test 账号控制（2026-09-08）

- 取消全局禁用语言开关，登录后的非 Test 账号在账号菜单显示中文/English，并保存一年期语言 Cookie；Test 账号隐藏入口、固定默认英文，服务端拒绝它提交语言切换并忽略旧中文 Cookie。登录前保持不显示切换入口。与照片显示共用精确测试邮箱域判定，不按显示姓名或设备识别。
- 类型检查、Lint、构建通过，照片策略回归测试通过；独立本机数据库和浏览器实测普通账号切换中文、刷新后保留中文、切回英文。HTTP 验证普通账号遵循中文 Cookie，Test 账号忽略中文 Cookie 且不输出语言控件。未进行原生 iPhone 实机验收，未提交推送或部署。

- iPhone 17 Pro 模拟器独立 App 的线路入口、智能调整和返回流程已验证，均留在原生容器；未验证物理真机及真实语音转写。证据保存在 `.local-data/rescheduling-verification/`。

### 三校名单及学生照片录入（2026-09-08）

### Cumberland / Cherry Chase 放学时间补录（2026-09-08）

- 补录此前名单导入时遗漏的两校时间：TK–G3 周一至周四 14:30、周五 12:45；G4–G5 周一至周五 14:30。当前 Fall 2026 学期各新增三条年级/星期规则。
- 根据九月表格补录每校两项日期安排：9/1–9/4 TK 11:35（其他年级沿用常规）；9/8 TK 11:35、K–G5 提前至 12:45。9/9 起 TK 沿用常规规则，9/7 既有停课保留。未把纸上未展示的八月 TK 过渡时间作推断。
- 通过现有学校设置校验保存，备份后先事务回滚预演，再写入生产；30 个日期/年级组合验证通过，提交后只读确认每校三条规则与两项新增日期安排。未改学生、司机车辆、现有线路或执行记录。

### Cumberland 特殊接送提醒复核（2026-09-08）

- 学校接送要求补入周五分批提醒及固定不接送名单：常规周五共 19 人，其中 17 人 12:45、两名 G4 学生 14:30，临时请假另计；两名学生须在学校门口或 office 等候，不要乱跑。同步补全对应个人备注，中英文均记录。
- 已核对每周四 1 人不接、每周五 4 人不接，原结构化数据正确。纸上 9/4 另外三处手写叉号仅属于该日标记，未推定为每周规则，也未补造历史执行事实。修改前保存私有备份，事务提交后只读复核学校提醒，人数校验 17+2 通过。

### 司机拍照辅助接车（2026-09-08）

司机行程信息与学生清单之间增加相机图标：预览 → 拍摄定格 → 姓名/未识别标记 → 确认后批量 Pickup。取消、重拍不写入，确认沿用共享名单并发校验并记录地点。本机 WASM 模型负责照片比对；不向后台或外部 AI 上传车内照片，安全带识别暂不启用。实现、数据边界与尚待 iPhone 实机验收项目见 `docs/pickup-camera.md`。本机语音改造仍按 `docs/pending-local-speech.md` 单独待办。

### 司机学生信息不提供家长联系方式（2026-09-08）

司机接送学生清单不显示家长联系信息，也不显示“家长联系方式待补充”。司机普通行程查询和共享名单刷新均在服务端清空家长姓名、电话字段，避免把联系方式发送到司机浏览器或 App。管理员学生管理和行程中的家长联系方式维持可见；学生请假和接送备注继续保留。

### iPhone 插件与权限预备（2026-09-08）

相机/相册、原生定位及 App 内浏览插件已同步至 Xcode 的 Swift Package 与注册清单。补齐语音识别用途说明，以及 Geolocation 插件要求的定位用途说明；新增 `ios:check`，`ios:sync` 自动检查配置。当前网页定位流程与本机语音待办没有因此自动替换，详见 `docs/ios-capabilities.md`。新增原生能力需重新编译安装 App。

### 本机 OCR 原生接口（2026-09-08）

新增 Apple Vision 文字识别原生桥接 `KidLoopOCR` 和 `src/lib/native-ocr.ts`，支持设备可用的中英文语言、文字内容/置信度/位置框返回，仅处理传入图片，不上传。接口已纳入 iPhone 构建及桥接注册检查；独立 OCR 业务页面未新增。详情见 `docs/ios-capabilities.md`。

### 行程完成后锁定孩子状态（2026-09-08）

- 整体行程或所属线路分段完成后，孩子状态只读，不再提供撤销或其他状态操作。普通与共享接送均由服务端事务校验，旧页面提交同样拒绝；既有接送时间、完成记录和操作历史保留。尚未完成的分段仍可正常操作。

### 日程日期区分（2026-09-09）

- 司机周/月日程按洛杉矶运营日期区分过去、今天与未来：过去为灰色，今天为深绿色并标注“今天 / Today”，未来为浅蓝色；选中日期使用独立边框。实心/空心小点分别表示有行程/无行程，时间分类与任务状态互不覆盖。下方日期卡片同步显示过去、今天或未来。
- 类型检查、Lint、生产构建及 4 项日程/日期测试通过。使用实际日期选择组件的静态渲染进行 390px/1280px、中英文、周/月共 8 组浏览器样式检查，无横向溢出，查看过去日期时仍保留今天标识；未进行完整登录交互或 iPhone 实机验收。未提交、推送或部署。

### 线路、自动名单及每日接送核对（2026-09-09）

- 线路仅设置开始时间、学校/课外班站点、适用日期及司机车辆；名单按学校、年级放学规则和后续课外班自动带入。学生行只有勾选、姓名和年级，不重复设置上下车站或地址，途经其他学校不改变学生关联。
- 共享归属学校接送批次；参与线路自动使用同一名单，排除例外同步。删除旧的逐学生站点配置、主/伙伴线路配置、独立每日共享创建入口及其数据库表。容量按路段和共享实际接送检查。
- 管理端当天概览提示异常，新增每日接送核对周/月日历。未来读取试算摘要，详情只加载所选日期；试算不创建正式车次。司机周/月保持过去、今天、未来的独立样式。
- 生产已应用迁移 030、031，按用户授权删除 53 个旧车次、648 条学生运行安排、52 个排班及 58 条状态历史；今天重建 2 个车次、23 名学生安排。57 名学生及学校、课外班、家长、照片、司机车辆、校历等输入经事务指纹复核未变。
- Little Tree 周五第 1 条线由 Chen 驾驶，两个车辆共享 Cumberland 的 16 名当天候选；Chen 线路另接 Cherry Chase 5 名学生。实际人数随校历及不接送规则变化。周五仍有 8 名未安排学生，核对界面如实显示，不补造安排。
- 61 项回归测试、类型检查、Lint、生产构建通过；隔离数据库 Chrome/WebKit 覆盖自动名单、取消勾选、保存、周/月和手机布局。线上 WebKit 复核表单和周/月页面无脚本错误，完整页面两次复核约 1.9–2.9 秒；这包含网络与渲染，不是试算 CPU 耗时，也不构成设备性能保证。
- 已部署 https://kid-loop.vercel.app；本轮代码、迁移、测试和文档纳入同一提交。iPhone 17 Pro 模拟器的线路 → 每日核对 → 日期切换 → 返回线路已验证全程留在原生容器；物理 iPhone 的完整操作验收未完成。

### App Store 权限说明修复（2026-09-11）

英文审核界面出现中文系统权限说明的问题已在原生配置中修复：全部 7 项权限以英文为默认文案，增加英文和简体中文 InfoPlist.strings 并纳入构建。系统弹窗遵循 iOS App 语言，网页 Cookie 不会即时改变系统语言。需上传新 build 后复审，验证状态与操作见 `docs/ios-capabilities.md`。

### 隐私政策与支持网页（2026-09-11）

保留公开英文 `/privacy.html`、`/support.html`，新增简体中文 `/privacy-zh.html`、`/support-zh.html`。更新接送操作位置、家长提供参考照片后的本地辅助识别、可选云端智能排班/语音、保留与删除请求说明；联系人沿用 Li Chen / chenli0741@gmail.com。页面通过同站同窗口链接切换语言与查看帮助，不唤起邮件客户端；不要求登录。网页内容不代表已填写 App Store Connect 隐私问卷。详见 `docs/privacy-policy.md`。
